# Copilot Instructions

## 项目指南
- For the first prototype version, each agent must produce only one kind of output, outputs should be as structured as possible in JSON or Markdown, and the CTO agent must only orchestrate rather than doing others' work.